apiVersion: v1
kind: Secret
metadata:
  name: meeting-api-gateway-secrets
  namespace: meeting-app
type: Opaque
stringData:
  # Database connection - use your actual Supabase URL
  DATABASE_URL: "postgresql://postgres.gwbwliafqmjhpoceofub:${SUPABASE_PASSWORD}@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

  # JWT secrets
  JWT_ACCESS_TOKEN_SECRET: "${JWT_ACCESS_TOKEN_SECRET}"
  JWT_REFRESH_TOKEN_SECRET: "${JWT_REFRESH_TOKEN_SECRET}"
